services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3001:3000"
    volumes:
      - .:/rails
      - bundle:/usr/local/bundle
    environment:
      - BINDING=0.0.0.0
    tty: true
    stdin_open: true

volumes:
  bundle:
